import React from 'react';

const Quiz = () => {
  return (
    <div>
      - Criar um estado local-useState- para controlar a pergunta atual.
      - Salva a pontuação do usuário
      - criamos um button para avançar para próxima pergunta
    </div>
  );
};

export default Quiz;